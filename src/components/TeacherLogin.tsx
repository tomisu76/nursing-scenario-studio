export function TeacherLogin({onLogin,onBack}:{onLogin:(e:string,p:string)=>void;onBack:()=>void}){return <form onSubmit={(e)=>{e.preventDefault();const f=new FormData(e.currentTarget);onLogin(String(f.get('email')),String(f.get('password')));}}><button type='button' onClick={onBack}>Back</button><input name='email' placeholder='email'/><input name='password' type='password' placeholder='password'/><button>Login</button></form>}
