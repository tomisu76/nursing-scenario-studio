import type { StudioScenario } from '../types';
export function TeacherDashboard({email,scenarios,onCreate,onEdit,onPreview,onDelete,onToggle,onLogout,onBack}:{email:string;scenarios:StudioScenario[];onCreate:()=>void;onEdit:(s:StudioScenario)=>void;onPreview:(s:StudioScenario)=>void;onDelete:(s:StudioScenario)=>void;onToggle:(s:StudioScenario)=>void;onLogout:()=>void;onBack:()=>void}){return <div><button onClick={onBack}>Home</button><p>{email}</p><button onClick={onLogout}>Sign out</button><button onClick={onCreate}>Create</button>{scenarios.map(s=><div key={s.id}><b>{s.title}</b><button onClick={()=>onEdit(s)}>Edit</button><button onClick={()=>onPreview(s)}>Preview</button><button onClick={()=>onToggle(s)}>{s.is_published?'Unpublish':'Publish'}</button><button onClick={()=>onDelete(s)}>Delete</button></div>)}</div>}
